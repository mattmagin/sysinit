# mkinitcpio hooks for LUKS encryption with Plymouth
# This file is placed in /etc/mkinitcpio.conf.d/ and adds hooks for disk encryption
# Hook order is critical: plymouth must come before encrypt to show password prompt

HOOKS=(base udev plymouth encrypt keyboard keymap autodetect modconf block filesystems fsck)
