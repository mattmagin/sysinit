# Btop configuration — managed by chezmoi
# Point to the chezmoi-generated theme
color_theme = "~/.config/btop/themes/chezmoi.theme"

# Show per-core CPU usage
shown_boxes = "cpu mem net proc"
proc_sorting = "cpu lazy"
proc_tree = false
proc_per_core = true
update_ms = 2000
